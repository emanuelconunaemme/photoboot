// Placeholder. Will be regenerated from Supabase via:
//   pnpm supabase gen types typescript --local > packages/shared/src/database.types.ts
// Run after each migration. Re-export from index.ts once populated.

export type Database = unknown;
